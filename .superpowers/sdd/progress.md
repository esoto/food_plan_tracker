Slice 3 ledger — plan: docs/superpowers/plans/2026-08-30-wellness-slice3-food-flag.md
Task 2: complete (commits 1a62df8..77d4d04 incl. nav-order fix, re-review clean)
Task 3: complete (Today/Days dashboard gating — macro hero, logged foods, right-now CTA, day/plan toggle gated; today/_rules left ungated — mixed content, gating it would ragged the lg grid)
Task 3: complete (commit 64a72cf, review clean — _rules left ungated by judgment, day_toggle is plan-only so gating whole partial correct)
Task 4: complete (Settings gating — plan macro targets + per-meal targets sections gated, @plans skipped when off, trailing explainer copy made conditional; notifications-alone-row layout for non-admin left as-is — pre-existing, unaffected by the flag)
PROCESS: task gates must run FULL suite (spec/requests-only gate let Task 3 break system specs silently — fixer dispatched)
Task 4: complete (commit 8fc65af, review clean — enabled copy verbatim, no nil-crash paths)
System-spec followup: complete (commit 4737385 — S4a/S4e enabled-user, invite S2 assertion now codifies food-off first-run; full suite 990/0)
Task 5: complete (Notifications + reminder job — NotificationsController @plan nil'd when food off, reusing existing `<% if @plan %>` guard so no view edit needed; UserReminderJob gates fire_meal_reminders on user.food_tracking_enabled? per-user inside Current.set block, supplement reminders unaffected; ReminderPreferencesController write path left ungated by judgment — a disabled user POSTing a meal pref creates a harmless unused row since the job never reads it for them; full suite 994/0, 5 pre-existing Tenantable pendings)
Task 5: complete (commit 240dfbc, review clean — no no-plan empty-state leak, job guard per-user, pref-writes inert-verified)
Task 6: complete (REST API guard — Api::Concerns::RequiresFoodTracking before_action on the 8 food controllers, 403 { error: "food_tracking_disabled" }; shared example "food-gated endpoint" included in each; habits control spec added; goals/weight/supplements/habits/weekly_summary left ungated. Broad RED from pre-existing api/v1 specs (default food_tracking_enabled: false) fixed at the single stub_api_token helper rather than per-file; 3 unrelated security specs (deactivation_gating, mass_assignment_isolation, two_user_isolation_journey) that mint tokens outside stub_api_token needed a targeted per-test food_tracking_enabled: true bump so the new gate doesn't shadow what they actually test. Full suite 1011/0, 5 pre-existing Tenantable pendings.)
Task 6: complete (commits b112c9a..f43e86e incl. vacuous-PATCH-spec fix, review clean — callback order + stub ordering verified)
Task 7: complete (MCP guard — tagged exactly 17 food TOOLS entries with food: true, get_weekly_summary deliberately untagged per spec carve-out; call_tool guard returns the exact in-band tool_error message "Food tracking is not enabled for this account" before dispatch; tool_descriptors untouched, tools/list still returns all 31 names for a disabled user (RED-verified by reverting the controller change and confirming only the guard-dependent example failed). mcp_spec.rb's `let(:user)` predates the flag and defaulted to food_tracking_enabled: false — bumped to true in the top-level before block, mirroring ApiHelpers#stub_api_token, so pre-existing food-tool examples keep passing; new "food tracking disabled" describe block covers log_food (isError + exact message + no LoggedFood created), list_habits (unaffected), get_weekly_summary (unaffected), and tools/list (still 31 names). Full suite 1015/0, 5 pre-existing Tenantable pendings, rubocop clean.)
Task 7: complete (commit 35c6ca1, review clean — 17 tags exact, guard at :127, no wire leak)
Task 8: complete (Admin toggle — `enable_food_tracking`/`disable_food_tracking` member routes + controller actions calling the Task-1 mutators, redirect with flash notice; deliberately NOT added to `forbid_self` since self-toggle is safe; "Food on"/"Food off" pill next to role pills (emerald/slate, matching the Member-neutral vs. Reactivate-emerald palette rather than Deactivated's rose warning tone since off is a normal default state, not an alarm); one opposite-action `button_to` per user beside Promote/Demote, shown unconditionally including for the acting admin (no `!= current_user` guard, unlike deactivate/destroy/demote). Actual admin spec file is `spec/requests/admin/users_spec.rb` (plan referenced `users_controller_spec.rb` — doesn't exist; mirrored the real promote/demote patterns there instead). Full suite 1022/0, 5 pre-existing Tenantable pendings, rubocop clean on touched .rb.)
